# @package _global_
defaults:
  - override /dataset: mb
  - override /dataloader: mb
  - override /model: mb_mlp
  - override /pmf_trainer: mb

task: mb_ub
n_nodes: 1
n_dim: 1
num_samples: 20000
batch_size: 256
epochs: 1000

data_path: ${hf:${hf_repo_id},MB/unbiased/data.npz}
target_path: ${hf:${hf_repo_id},MB/unbiased/data.npz}
energy_params_path: ${hf:${hf_repo_id},MB/PMF_B/final_params.pkl}
